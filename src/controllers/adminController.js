//  администратор может просматривать список всех пользователей системы. Эндпоинт для этого будет `GET /api/admin/users`. Доступ к нему должен быть только у роли `admin`.
/*const getAllUsers = async (req, res) => {
    //
    const userController = require('./userController');
    userController.getAllUsers(req, res);
};
        
 module.exports = {
    getAllUsers
};   */